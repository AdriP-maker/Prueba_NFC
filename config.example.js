// config.example.js — Plantilla de configuración
// Copia este archivo a config.js y pon tu token real
// config.js está en .gitignore y nunca se sube al repo
window.ENV = {
  GIST_TOKEN: 'your_github_token_here',
};
